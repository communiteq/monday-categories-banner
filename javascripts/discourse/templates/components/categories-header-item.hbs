<div class="categories-header-item {{if category.uploaded_logo.url "has-logo" "no-logo"}} category-{{category.slug}}">

{{#if category.uploaded_logo.url}}
<div class="category-logo">
        {{cdn-img
          src=category.uploaded_logo.url
          class="category-logo"
          width=category.uploaded_logo.width
          height=category.uploaded_logo.height
          alt=""
        }}
</div>
{{/if}}

  <h3 class="category-title">
    <a class="category-title-link" href={{category.url}}>
      <div class="category-icon-name">
        {{category-title-before category=category}}
        {{#if category.read_restricted}}
          {{d-icon "lock"}}
        {{/if}}
        <span class="category-name">{{dir-span category.name}}</span>
      </div>
    </a>
  </h3>
  <div class="category-description">
       <div class="cooked" innerHTML={{category.description}}></div>
  </div>
</div>
